apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: price-service
  name: price-service
spec:
  endpoints:  
  - interval: 30s
    port: 8080-tcp
    scheme: http
    path: /actuator/prometheus
  selector:
    matchLabels:
      app: price-service